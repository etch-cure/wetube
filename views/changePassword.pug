extends layout/main.pug

block content
    .form-container
        form(action=routes.editProfile, method="post")
            input(type="password", name="password", placeholder="Current Password", required=true)
            input(type="password", name="password", placeholder="New Password", required=true)
            input(type="password", name="password2", placeholder="Verify New Password", required=true)
            input(type="submit", value="change")